generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model uploadedFile {
  id        String   @id
  name      String
  data      Bytes
  updatedAt DateTime
}
